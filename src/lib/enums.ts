export enum FitInType {
    DEFAULT = 'DEFAULT',
    FULL = 'FULL',
    ADAPTIVE = 'ADAPTIVE'
}

export enum VerticalPosition {
    TOP = 'TOP',
    MIDDLE = 'MIDDLE',
    BOTTOM = 'BOTTOM'
}

export enum HorizontalPosition {
    LEFT = 'LEFT',
    CENTER = 'CENTER',
    RIGHT = 'RIGHT'
}
